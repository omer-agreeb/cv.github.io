sudo: required
dist: trusty
before_install:
- sudo apt-get -qq update && sudo apt-get install -y --no-install-recommends texlive-full
tex-config:
- packages= academicons
script:
- xelatex -interaction=nonstopmode -halt-on-error resume.tex
